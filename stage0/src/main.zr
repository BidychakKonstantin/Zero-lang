    use "tokens.zr"
    use "std.zr"
    use "lexer.zr"

    add(nat64 a, nat64 b) -> nat64 {
    ret a + b
    }

    main() {
        
        chr* str = 0
        lexer_init(str,10)
        nat16 a = 0
        bool ds = true
        nat8 bc = is_end()
        nat64 res = add(10,10)
      //  write_str("     mov rdi, 10")
        write_bytes(str,a)
        if a == 0 || a == 1
        {
            a = a + 1
        }
        rep 10 {
            a = a + 1
            print(a)
        }

        ret a
    }