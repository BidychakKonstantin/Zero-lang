use "tokens.zr"

use "lexer.zr"

add(nat64 a, nat64 b) -> nat64 {
  ret a + b
}

main() {
    nat8 xa = TOK_EOF
    chr* str = NULL
    nat16 a = 0
    nat8 bc = is_end()
    nat64 res = add(10,10)

    rep 10 {
        a = a + 1
        print(a)
    }

    ret a
}